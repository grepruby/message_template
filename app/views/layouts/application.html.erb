<!DOCTYPE html>
<html>
<head>
  <title><%= content_for?(:title) ? yield(:title) : "MessageManagement" %></title>
  <%= stylesheet_link_tag "application", :media => "all" %>
  <style type="text/css">
      body {
        padding-top: 60px;
        padding-bottom: 40px;
      }
      .sidebar-nav {
        padding: 9px 0;
      }
    </style>
  <%= csrf_meta_tags %>
</head>
<body>
<div class="navbar navbar-inverse navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container-fluid">
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <a class="brand" href="#">Demo Message Management</a>
      <div class="nav-collapse collapse">
        <p class="navbar-text pull-right">
          <% if user_signed_in? %>	
            Logged in as <a href="#" class="navbar-link"><%= current_user.email%></a> 
            <%= link_to('Logout', destroy_user_session_path, :method => :delete,:class=>"navbar-link") %>
          <%else%>
          	<%= link_to('Log In', new_user_session_path,:class=>"navbar-link") %>

          <%end%>
        </p>
        <ul class="nav">
          <li class="active">
          	<%= link_to('Home', root_path,:title=>"Home") %>
          </li>
          <%= render "shared/sidebar_menu"%>
        </ul>
      </div><!--/.nav-collapse -->
    </div>
  </div>
</div>

<div class="container-fluid">
  <div class="row-fluid">
    <div class="span3">
      <div class="well sidebar-nav">
      	  <ul class="nav nav-list">
            <li class="nav-header">Sidebar</li>
            <li class="active">
          	  <%= link_to('Home', root_path,:title=>"Home") %>
            </li>
            <%= render "shared/sidebar_menu"%>
          </ul>
      </div><!--/.well -->
    </div><!--/span-->
    <div class="span9">
      <div class="hero-unit">
      	<%= yield%>  
      </div>
    </div><!--/span-->
  </div><!--/row-->

  <hr>

  <footer>
    <p>© Company 2012</p>
  </footer>

</div><!--/.fluid-container-->
<%= javascript_include_tag "application" %>
</body>
</html>
